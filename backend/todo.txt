TODO:

- Implement non-deterministic sampling
- Client disconnect mid-generation leaves an orphaned worker thread still burning CPU. Need a cancellation signal (e.g. a threading.Event the async side sets on disconnect, worker checks between tokens).
- Refactor the generation loop to use GPU; accounting for all tensor movements from host to device & vice-versa
- Send cumulative KV cache of the entire sequence at the completion event
- create model registry