PROMPTS:


I have few things I am confused about. I want to discuss those with you. I am caught up in memory management of KV cache. I created this helper method to calculate KV cache, tensor contained in past_key_values variable, DynamicCache object internally in transformers library. Ok, that's fine. So when I run this `SmolLM2-360M-Instruct` model I see that KV cache for each token is roughly 36 KB, makes sense, right. Now, I also have a method to compute the true memory footprint of my root process to see how much memory is getting consumed during generation. I wanted it to be accurate. So rather than relying on approxiamtion of CLI tools like `htop -p` I get into the proc/<pid> directory of OS and calculte the current usage from `smaps_rollup` file. After each token generation I print the memory footprint as you can see in the code.

All these efforts are motivated by the fact that I saw growth in memory footprint of the root process in a highly non-linear way which cannot be explained theoretically by how much KV cache I was generating. Let me give you some particular observation. I load the `SmolLM2-360M-Instruct` and give a total generation sequence length of 50 tokens. After the 50-th token generation, the KV cache of the entire sequence comes out to be: 
`Seq length: 50, KV cache: 1.95 MB` as per my stdout prints which also aligns with my theoretical calculation.

But if you see the memory footprint grows multiple times of this during the generation process of these 50 tokens. Below is the stdout print which informs it:
`Generation process memory diff: 224.00390625 MB`

I thought may be it is storing the KV cache of older sequences while I am running the auto-regressive loop. So I also calculated this cumulative sum of KV cache sizes for all input sequences to `model` callable. I see this in stdout:
`Cumulative sum of all KV cache tensor for the generation: 49.8046875` NOTE: unit is MBs

I see the process memory footprint is not even equal to this it is more than 4x of it. I can't explain this. All my analysis gave me this much peek into the problem. I want you to investigate this. One quirky thing I noticed is this; in the next prompt if I give the sequence length less than what's ever given in history, no growth in memory footprint is happing. But if I enter the output sequence length to be greater than what has ever happened in history process memory footprint starts growing in a very non-linear fashion as show above. I am attaching the stdout of one generation for you to get better insight on the problem in `logs.txt` file.

---
Let me explain whether I understood correctly. You are saying that when I am invoking the `model` callable using input_seq_argument it is doing a forwards pass on the model under the hood using `torch`. Your claim is that the activations that is getting generated per forward pass get stored, i.e., those tensors have `requires_grad` attribute true which means they become part of the computational graph with the expectation that they would be used for doing a backward pass in future which we never do. So the growth in memory can be explained by accounting for activations during each forward pass. After the `.generate()` method finishes execution and get scoped out, although the computational graph that got built is discarded, the memory allocated by the OS initially stays in reserve pool rather being freed. So for further allocations reserve pool is used rather than fresh allocations by the OS. Because we are passing `past_key_values` of the previous sequence in the next generation, it is also becoming the part of the computational graph, so the cumulative sum of KV cache is getting stored. Check if what I understood is exactly aligns to your claim or not. Tell me if anywhere I misalign from what you wanted to express.